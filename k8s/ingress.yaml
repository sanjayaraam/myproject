apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sjproject-ingress
spec:
  rules:
  - host: sj.project
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sjproject-service
            port:
              number: 80
